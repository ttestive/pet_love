import { LoginUsuario } from '@/app/admin/login/novo/page';

// O segredo está nesta linha: "export default"
export default function AdminLoginPage() {
    return (
        <LoginUsuario />
    );
}
